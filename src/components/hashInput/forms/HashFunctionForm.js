import React, { useState } from "react";

import "./HashFunctionForm.css";

const HashFunctionForm = (props) => {
  return (
    <div>
      <button type="submit">Create Random Hash</button>
    </div>
  );
};

export default HashFunctionForm;
